{
  "name": "clipper-studio-workspace",
  "version": "1.0.0",
  "scripts": {
    "backend": "dev_env=. && kill -9 $(lsof -t -i:8000) 2>/dev/null || true; [ -f venv/bin/activate ] && . venv/bin/activate; export PYTHONPATH=$PYTHONPATH:$(pwd)/backend && uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload --h11-max-incomplete-event-size 104857600",
    "frontend": "npm run dev --prefix frontend"
  }
}
